import { Page } from "./Page.slint";
import { VerticalBox } from "std-widgets.slint";

export component ValidatePage inherits Page {
    title: "検証";

    Text {
        x: 0;
        color: black;
        text: "Body";
    }
}
