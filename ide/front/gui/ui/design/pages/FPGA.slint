import { Page } from "./Page.slint";
import { VerticalBox } from "std-widgets.slint";

export component FPGAPage inherits Page {
    title: "FPGA 書き込み";

    Text {
        x: 0;
        color: black;
        text: "Body";
    }
}
