import { Page } from "./Page.slint";
import { VerticalBox } from "std-widgets.slint";

export component ProjectPage inherits Page {
    title: "プロジェクト作成";

    Text {
        x: 0;
        color: black;
        text: "Body";
    }
}
