[package]
name = "fpga_server"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = { workspace = true }
thiserror = { workspace = true }
server_job = { path = "../server_job/job" }
server_worker = { path= "../server_worker/worker" }
